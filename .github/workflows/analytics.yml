# Analytics + learning — pull next-day metrics, score hooks (Phase 4).
# SKELETON: manual-trigger only. Not part of the Phase-1 MVP (rule 9: stay in-phase).
name: analytics

on:
  workflow_dispatch: {}
  # schedule:
  #   - cron: "0 6 * * *"   # UTC, next-day metrics pull — enable in Phase 4

jobs:
  analytics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - name: Pull metrics (Phase 4)
        run: echo "Phase 4 — analytics not implemented yet (see docs/06-roadmap.md)."
